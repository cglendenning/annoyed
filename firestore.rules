rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only read/write their own data
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    match /annoyances/{annoyanceId} {
      // Separate read and write for better security
      allow read: if request.auth != null && 
                     resource.data.uid == request.auth.uid;
      allow update, delete: if request.auth != null && 
                               resource.data.uid == request.auth.uid;
      allow create: if request.auth != null && 
                      request.resource.data.uid == request.auth.uid;
    }
    
    match /suggestions/{suggestionId} {
      // Separate read and write for better security
      allow read: if request.auth != null && 
                     resource.data.uid == request.auth.uid;
      allow update, delete: if request.auth != null && 
                               resource.data.uid == request.auth.uid;
      allow create: if request.auth != null && 
                      request.resource.data.uid == request.auth.uid;
    }
    
    match /coaching/{coachingId} {
      allow read: if request.auth != null && 
                     request.auth.uid == resource.data.uid;
      allow create: if request.auth != null && 
                      request.auth.uid == request.resource.data.uid;
      // Allow users to delete their own coaching records (for GDPR compliance)
      allow delete: if request.auth != null && 
                       request.auth.uid == resource.data.uid;
    }
    
    match /events/{eventId} {
      allow read: if request.auth != null && 
                     request.auth.uid == resource.data.uid;
      allow create: if request.auth != null && 
                      request.auth.uid == request.resource.data.uid;
      // Allow users to delete their own event records (for GDPR compliance)
      allow delete: if request.auth != null && 
                       request.auth.uid == resource.data.uid;
    }
    
    match /llm_cost/{costId} {
      // Only Firebase Functions can create cost records
      // Users can read their own cost records
      allow read: if request.auth != null && 
                     request.auth.uid == resource.data.uid;
      // Block client-side creation - only Functions can create cost records
      allow create: if false;
      // Allow users to delete their own cost records (for GDPR compliance)
      allow delete: if request.auth != null && 
                       request.auth.uid == resource.data.uid;
    }
  }
}


